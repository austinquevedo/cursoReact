const firstName = 'Agustin';
const lastName = 'Quevedo';

const mensaje = `O'Neal es es apellido de  ${firstName} ${lastName}`;

const fullName = `${firstName} ${lastName}`;

console.log({ fullName, mensaje });
